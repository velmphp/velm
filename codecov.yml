coverage:
  status:
    project:
      default:
        target: 78%
        threshold: 1%
    patch:
      default:
        target: 78%

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: false
